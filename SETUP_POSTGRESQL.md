# วิธีติดตั้งและรันโปรเจคด้วย PostgreSQL

## ขั้นตอนที่ 1: สร้าง Database

### วิธีที่ 1: ใช้ pgAdmin (GUI)
1. เปิดโปรแกรม **pgAdmin**
2. คลิกขวาที่ **Databases** → **Create** → **Database**
3. ตั้งชื่อ: `shark_coder`
4. คลิก **Save**

### วิธีที่ 2: ใช้ Command Line (psql)
เปิด Command Prompt หรือ PowerShell แล้วรัน:

```cmd
psql -U postgres
```

จากนั้นพิมพ์:

```sql
CREATE DATABASE shark_coder;
\q
```

## ขั้นตอนที่ 2: ตั้งค่า .env

ตรวจสอบไฟล์ `.env` ว่ามีค่าดังนี้:

```env
PORT=8080
DB_USER=postgres
DB_HOST=localhost
DB_PASSWORD=postgres
DB_PORT=5432
DB_NAME=shark_coder
```

**หมายเหตุ:** 
- `DB_PASSWORD` ต้องตรงกับรหัสผ่านที่คุณตั้งตอนติดตั้ง PostgreSQL
- ถ้าคุณตั้งรหัสผ่านอื่น ให้แก้ไขที่ `DB_PASSWORD=รหัสผ่านของคุณ`

## ขั้นตอนที่ 3: รัน Backend

เปิด Command Prompt หรือ Terminal แล้วรันคำสั่ง:

```cmd
node index.js
```

ถ้าสำเร็จจะขึ้นข้อความ:
```
SharkCoder API online on port 8080
```

**Backend จะสร้างตารางทั้งหมดอัตโนมัติ!** ไม่ต้อง import SQL file

## ขั้นตอนที่ 4: รัน Frontend

Frontend รันอยู่แล้วที่ http://localhost:3000

ถ้ายังไม่ได้รัน ให้เปิด Terminal ใหม่แล้วรัน:

```cmd
cd frontend
npm run dev
```

## ✅ เสร็จสิ้น!

- **Backend API:** http://localhost:8080
- **Frontend:** http://localhost:3000
- **Health Check:** http://localhost:8080/health

## 🔧 แก้ปัญหา

### ถ้า Backend แสดง "ECONNREFUSED"
1. ตรวจสอบว่า PostgreSQL service กำลังรันอยู่
   - Windows: เปิด Services → หา "postgresql" → ต้องเป็น "Running"
2. ตรวจสอบว่าสร้าง database `shark_coder` แล้ว
3. ตรวจสอบ username/password ใน `.env`

### ถ้า Backend แสดง "password authentication failed"
1. แก้ไข `DB_PASSWORD` ใน `.env` ให้ตรงกับรหัสผ่าน postgres ของคุณ
2. หรือรีเซ็ตรหัสผ่าน postgres:
   ```cmd
   psql -U postgres
   ALTER USER postgres PASSWORD 'postgres';
   \q
   ```

### ถ้า Frontend แสดง "Failed to fetch"
1. ตรวจสอบว่า Backend รันอยู่ที่ port 8080
2. เปิด http://localhost:8080/health ดูว่าตอบกลับมาไหม
3. ตรวจสอบ Console ของ Browser (F12) ดู error

### ถ้าไม่สามารถเชื่อมต่อ PostgreSQL
1. ตรวจสอบว่า PostgreSQL ติดตั้งแล้ว:
   ```cmd
   psql --version
   ```
2. ถ้ายังไม่ได้ติดตั้ง ดาวน์โหลดจาก: https://www.postgresql.org/download/windows/

## 📝 ข้อมูล Admin เริ่มต้น

หลังจากรัน Backend ครั้งแรก ระบบจะสร้าง Admin account อัตโนมัติ:

- **Email:** `admin@sharkcoder.dev`
- **Password:** `admin1234`

ใช้ข้อมูลนี้เข้าสู่ระบบที่ http://localhost:3000/login

## 🎯 ขั้นตอนย่อ (Quick Start)

```cmd
# 1. สร้าง database
psql -U postgres -c "CREATE DATABASE shark_coder;"

# 2. รัน Backend
node index.js

# 3. รัน Frontend (Terminal ใหม่)
cd frontend
npm run dev
```

เสร็จแล้ว! เปิด http://localhost:3000
