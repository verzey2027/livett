# 🎨 คู่มือการใช้งาน Widget Overlays

## 📋 สารบัญ
1. [Widget ทั้งหมด](#widget-ทั้งหมด)
2. [วิธีเพิ่ม Widget ใน OBS Studio](#วิธีเพิ่ม-widget-ใน-obs-studio)
3. [การปรับแต่ง Widget](#การปรับแต่ง-widget)
4. [การทดสอบ Widget](#การทดสอบ-widget)

---

## 🎯 Widget ทั้งหมด

### 1. 🔔 Widget แจ้งเตือนโดเนท (Donation Alert)
แสดงแอนิเมชันเมื่อมีคนโดเนท พร้อมชื่อ จำนวนเงิน และข้อความ

**URL:**
```
http://localhost:3000/widgets/alert
```

**คุณสมบัติ:**
- แสดงชื่อผู้โดเนท
- แสดงจำนวนเงิน
- แสดงข้อความจากผู้โดเนท
- แอนิเมชันสวยงาม 5 วินาที
- ปรับแต่งสี ขนาดตัวอักษรได้

**ตัวอย่างการปรับแต่ง:**
```
http://localhost:3000/widgets/alert?textColor=#00ff00&fontSize=40
```

---

### 2. 🎯 Widget เป้าหมายการโดเนท (Donation Goal)
แสดงความคืบหน้าเป้าหมายการโดเนท พร้อม progress bar

**URL:**
```
http://localhost:3000/widgets/goal
```

**คุณสมบัติ:**
- แสดง progress bar แบบเรียลไทม์
- แสดงยอดปัจจุบันและเป้าหมาย
- แสดงเปอร์เซ็นต์ความคืบหน้า
- อัพเดทอัตโนมัติทุก 5 วินาที

**ตัวอย่างการปรับแต่ง:**
```
http://localhost:3000/widgets/goal?target=5000&title=เป้าหมายวันนี้&barColor=#ff00ff
```

---

### 3. 🏆 Widget การจัดอันดับ (Leaderboard)
แสดงรายชื่อผู้โดเนทอันดับต้นๆ แบบเรียลไทม์

**URL:**
```
http://localhost:3000/widgets/leaderboard
```

**คุณสมบัติ:**
- แสดงอันดับ 1-10 (ปรับได้)
- เหรียญทอง เงิน ทองแดง สำหรับ 3 อันดับแรก
- รวมยอดโดเนททั้งหมดของแต่ละคน
- อัพเดทอัตโนมัติทุก 10 วินาที

**ตัวอย่างการปรับแต่ง:**
```
http://localhost:3000/widgets/leaderboard?limit=5&title=Top 5 Supporters
```

---

## 🎬 วิธีเพิ่ม Widget ใน OBS Studio

### ขั้นตอนที่ 1: เปิด OBS Studio
เปิดโปรแกรม OBS Studio ของคุณ

### ขั้นตอนที่ 2: เพิ่ม Browser Source
1. ในส่วน **Sources** ให้กดรูป **+** ด้านล่าง
2. เลือก **Browser**
3. ตั้งชื่อ Widget ของตัวเอง (เช่น "Donation Alert")
4. กด **OK**

### ขั้นตอนที่ 3: ตั้งค่า Browser Source
1. ในช่อง **URL** ให้วางลิงก์ Widget ที่คุณต้องการ
   ```
   http://localhost:3000/widgets/alert
   ```

2. ตั้งค่าขนาด:
   - **Width:** `1920`
   - **Height:** `1080`

3. เลือก **Shutdown source when not visible** (แนะนำ)

4. กด **OK**

### ขั้นตอนที่ 4: ปรับตำแหน่งและขนาด
1. Widget จะปรากฏใน OBS
2. ลากเพื่อย้ายตำแหน่ง
3. ลากมุมเพื่อปรับขนาด
4. คลิกขวา → Transform → เพื่อปรับแต่งเพิ่มเติม

### ขั้นตอนที่ 5: ทดสอบ Widget
1. เปิดเว็บไซต์ไปที่ http://localhost:3000/widgets
2. กดปุ่ม **"ทดสอบ Widget"** (สำหรับ Donation Alert)
3. ดูว่า Widget แสดงผลใน OBS หรือไม่

---

## ⚙️ การปรับแต่ง Widget

### Widget แจ้งเตือนโดเนท

| พารามิเตอร์ | คำอธิบาย | ค่าเริ่มต้น | ตัวอย่าง |
|------------|---------|------------|---------|
| `textColor` | สีข้อความ | `#ffffff` | `?textColor=#00ff00` |
| `bgColor` | สีพื้นหลัง | `rgba(0,0,0,0.8)` | `?bgColor=rgba(255,0,0,0.5)` |
| `fontSize` | ขนาดตัวอักษร | `32` | `?fontSize=48` |

**ตัวอย่าง:**
```
http://localhost:3000/widgets/alert?textColor=#00ff00&fontSize=40&bgColor=rgba(0,0,0,0.9)
```

---

### Widget เป้าหมายการโดเนท

| พารามิเตอร์ | คำอธิบาย | ค่าเริ่มต้น | ตัวอย่าง |
|------------|---------|------------|---------|
| `target` | เป้าหมาย (บาท) | `1000` | `?target=5000` |
| `title` | ชื่อเป้าหมาย | `เป้าหมายการโดเนท` | `?title=เป้าหมายวันนี้` |
| `barColor` | สี Progress Bar | `#06b6d4` | `?barColor=#ff00ff` |
| `bgColor` | สีพื้นหลัง | `rgba(0,0,0,0.8)` | `?bgColor=rgba(0,0,0,0.5)` |

**ตัวอย่าง:**
```
http://localhost:3000/widgets/goal?target=5000&title=เป้าหมายวันนี้&barColor=#ff00ff
```

---

### Widget การจัดอันดับ

| พารามิเตอร์ | คำอธิบาย | ค่าเริ่มต้น | ตัวอย่าง |
|------------|---------|------------|---------|
| `limit` | จำนวนอันดับ | `10` | `?limit=5` |
| `title` | ชื่อ Widget | `🏆 อันดับผู้สนับสนุน` | `?title=Top Donors` |
| `bgColor` | สีพื้นหลัง | `rgba(0,0,0,0.8)` | `?bgColor=rgba(0,0,0,0.5)` |

**ตัวอย่าง:**
```
http://localhost:3000/widgets/leaderboard?limit=5&title=Top 5 Supporters
```

---

## 🧪 การทดสอบ Widget

### วิธีที่ 1: ใช้ปุ่มทดสอบในเว็บ
1. เปิด http://localhost:3000/widgets
2. หา Widget แจ้งเตือนโดเนท
3. กดปุ่ม **"ทดสอบ Widget"**
4. ดูผลใน OBS

### วิธีที่ 2: โดเนทจริง
1. เปิด http://localhost:3000/donate
2. กรอกข้อมูลและโดเนท
3. Widget จะแสดงผลอัตโนมัติ

### วิธีที่ 3: ใช้ API โดยตรง
```bash
curl -X POST http://localhost:8080/api/donations/test \
  -H "Content-Type: application/json" \
  -d '{"donorName":"ผู้ทดสอบ","amount":100,"message":"ทดสอบ Widget"}'
```

---

## 💡 เคล็ดลับ

### 1. การจัดวาง Widget
- **Donation Alert:** วางตรงกลางหน้าจอ (เพื่อให้เด่น)
- **Donation Goal:** วางมุมบนขวา หรือ มุมล่างซ้าย
- **Leaderboard:** วางด้านข้างของหน้าจอ

### 2. การปรับแต่งสี
- ใช้สีที่เข้ากับธีมของคุณ
- ใช้ `rgba()` เพื่อความโปร่งใส
- ทดสอบสีก่อนไลฟ์จริง

### 3. ขนาดและตำแหน่ง
- ปรับขนาดให้เหมาะสมกับหน้าจอ
- อย่าให้ Widget บังเนื้อหาสำคัญ
- ทดสอบในหลายความละเอียด

### 4. ประสิทธิภาพ
- Widget อัพเดทอัตโนมัติ ไม่ต้องรีเฟรช
- ใช้ "Shutdown source when not visible" ใน OBS
- ปิด Widget ที่ไม่ใช้งาน

---

## 🔧 การแก้ปัญหา

### Widget ไม่แสดงผล
1. ตรวจสอบว่า Backend รันอยู่ (http://localhost:8080/health)
2. ตรวจสอบว่า Frontend รันอยู่ (http://localhost:3000)
3. ตรวจสอบ URL ใน OBS ว่าถูกต้อง
4. ลองรีเฟรช Browser Source (คลิกขวา → Refresh)

### Widget ไม่อัพเดท
1. ตรวจสอบ Console ใน Browser (F12)
2. ตรวจสอบว่า API ทำงานปกติ
3. ลองโดเนทใหม่อีกครั้ง

### Widget แสดงผลช้า
1. ลดจำนวน Widget ที่เปิดพร้อมกัน
2. ปิด Widget ที่ไม่ใช้งาน
3. ตรวจสอบ Network ว่าช้าหรือไม่

---

## 📞 ต้องการความช่วยเหลือ?

ถ้ามีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ Console ใน Browser (F12)
2. ตรวจสอบ Logs ของ Backend
3. ลองรีสตาร์ท Backend และ Frontend

---

**สนุกกับการสตรีม! 🎮🎉**
