# วิธีติดตั้งและรันโปรเจคด้วย XAMPP (MySQL)

## ขั้นตอนที่ 1: เปิด XAMPP
1. เปิดโปรแกรม XAMPP Control Panel
2. กด **Start** ที่ Apache และ MySQL
3. รอจนสถานะเป็นสีเขียว

## ขั้นตอนที่ 2: สร้าง Database
1. เปิดเบราว์เซอร์ไปที่ http://localhost/phpmyadmin
2. คลิก **New** (สร้างฐานข้อมูลใหม่)
3. ตั้งชื่อ: `shark_coder`
4. Collation: `utf8mb4_unicode_ci`
5. คลิก **Create**

## ขั้นตอนที่ 3: Import ตาราง
1. คลิกที่ database `shark_coder` ที่เพิ่งสร้าง
2. คลิกแท็บ **Import**
3. คลิก **Choose File** แล้วเลือกไฟล์ `setup_mysql.sql`
4. คลิก **Go** (ด้านล่างสุด)
5. รอจนขึ้นข้อความ "Import has been successfully finished"

## ขั้นตอนที่ 4: ตั้งค่า .env
ตรวจสอบไฟล์ `.env` ว่ามีค่าดังนี้:

```env
DB_USER=root
DB_HOST=localhost
DB_PASSWORD=
DB_PORT=3306
DB_NAME=shark_coder
```

**หมายเหตุ:** 
- ถ้า XAMPP ของคุณตั้งรหัสผ่าน MySQL ไว้ ให้ใส่ที่ `DB_PASSWORD=รหัสผ่านของคุณ`
- ปกติ XAMPP ไม่มีรหัสผ่าน (เว้นว่างไว้)

## ขั้นตอนที่ 5: รัน Backend
เปิด Command Prompt หรือ Terminal แล้วรันคำสั่ง:

```cmd
node index.js
```

ถ้าสำเร็จจะขึ้นข้อความ:
```
[Database] MySQL connected successfully!
[Migration] MySQL migrations completed successfully!
SharkCoder API online on port 8080
```

## ขั้นตอนที่ 6: รัน Frontend
Frontend รันอยู่แล้วที่ http://localhost:3000

ถ้ายังไม่ได้รัน ให้เปิด Terminal ใหม่แล้วรัน:

```cmd
cd frontend
npm run dev
```

## ✅ เสร็จสิ้น!
- Backend API: http://localhost:8080
- Frontend: http://localhost:3000
- phpMyAdmin: http://localhost/phpmyadmin

## 🔧 แก้ปัญหา

### ถ้า Backend ไม่รัน
1. ตรวจสอบว่า MySQL ใน XAMPP เปิดอยู่ (สีเขียว)
2. ตรวจสอบว่าสร้าง database `shark_coder` แล้ว
3. ตรวจสอบ username/password ใน `.env`

### ถ้า Frontend แสดง "Failed to fetch"
1. ตรวจสอบว่า Backend รันอยู่ที่ port 8080
2. เปิด http://localhost:8080/health ดูว่าตอบกลับมาไหม

### ถ้า Import SQL ไม่ได้
1. ลองคัดลอกโค้ดใน `setup_mysql.sql` ไปวางใน SQL tab ของ phpMyAdmin
2. กด **Go** เพื่อรัน

## 📝 ข้อมูล Admin เริ่มต้น
- Email: `admin@sharkcoder.dev`
- Password: `admin1234`
