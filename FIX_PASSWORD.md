# 🔐 แก้ปัญหารหัสผ่าน PostgreSQL

## ❌ Error: "password authentication failed for user postgres"

### สาเหตุ
รหัสผ่านใน `.env` ไม่ตรงกับรหัสผ่าน PostgreSQL ที่คุณตั้งไว้

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: แก้ไขไฟล์ .env (แนะนำ)

1. เปิดไฟล์ `.env` ด้วย Notepad:
   ```cmd
   notepad .env
   ```

2. แก้ไขบรรทัดนี้:
   ```env
   DB_PASSWORD=postgres
   ```
   เปลี่ยนเป็นรหัสผ่านที่คุณตั้งตอนติดตั้ง PostgreSQL

3. บันทึกไฟล์ (Ctrl+S)

4. รัน Backend ใหม่:
   ```cmd
   node index.js
   ```

---

### วิธีที่ 2: เปลี่ยนรหัสผ่าน PostgreSQL

ถ้าคุณลืมรหัสผ่าน หรืออยากเปลี่ยนเป็น `postgres`:

1. เปิด Command Prompt แบบ Administrator

2. รันคำสั่ง:
   ```cmd
   psql -U postgres
   ```

3. ถ้าเข้าได้ ให้พิมพ์:
   ```sql
   ALTER USER postgres PASSWORD 'postgres';
   \q
   ```

4. ถ้าเข้าไม่ได้ ให้แก้ไขไฟล์ `pg_hba.conf`:
   
   **ตำแหน่งไฟล์:**
   ```
   C:\Program Files\PostgreSQL\15\data\pg_hba.conf
   ```
   
   **แก้ไขบรรทัดนี้:**
   ```
   # IPv4 local connections:
   host    all             all             127.0.0.1/32            scram-sha-256
   ```
   
   **เปลี่ยนเป็น:**
   ```
   # IPv4 local connections:
   host    all             all             127.0.0.1/32            trust
   ```

5. Restart PostgreSQL service:
   - เปิด Services (กด Win+R → พิมพ์ `services.msc`)
   - หา "postgresql-x64-15" (หรือเวอร์ชันที่คุณติดตั้ง)
   - คลิกขวา → Restart

6. ตอนนี้รันคำสั่งใน ข้อ 2-3 อีกครั้ง

7. เปลี่ยน `pg_hba.conf` กลับเป็น `scram-sha-256`

8. Restart PostgreSQL service อีกครั้ง

---

### วิธีที่ 3: ใช้รหัสผ่านว่าง (ไม่แนะนำ)

ถ้า PostgreSQL ของคุณไม่มีรหัสผ่าน:

แก้ไขไฟล์ `.env`:
```env
DB_PASSWORD=
```
(เว้นว่างไว้)

---

## 🧪 ทดสอบการเชื่อมต่อ

หลังจากแก้ไขแล้ว ทดสอบด้วยคำสั่ง:

```cmd
psql -U postgres -d shark_coder
```

ถ้าเข้าได้แสดงว่ารหัสผ่านถูกต้อง!

พิมพ์ `\q` เพื่อออก

---

## 📝 รหัสผ่านที่ใช้บ่อย

ลองรหัสผ่านเหล่านี้:
- `postgres`
- `admin`
- `root`
- `password`
- `123456`
- (ว่างเปล่า)

---

## 🆘 ยังแก้ไม่ได้?

1. ตรวจสอบว่า PostgreSQL service รันอยู่
2. ลองถอนการติดตั้งและติดตั้งใหม่ (จดรหัสผ่านให้ดี!)
3. ใช้ pgAdmin เพื่อเชื่อมต่อและเปลี่ยนรหัสผ่าน

---

**หลังจากแก้ไขเสร็จ รันคำสั่ง:**
```cmd
node index.js
```

ถ้าเห็นข้อความ "SharkCoder API online on port 8080" แสดงว่าสำเร็จ! 🎉
